// import { ContextMock } from 'server/utils/testMocks';
// import { lnpayResolvers } from './resolvers';

jest.mock('logger');

describe('LnPay Resovlers', () => {
  describe('getLnPay', () => {
    test('success', () => {
      // const result = lnpayResolvers.Query.getLnPay(
      //   undefined,
      //   { amount: 123 },
      //   ContextMock
      // );

      expect('asd').toEqual('asd');
    });
  });
});
